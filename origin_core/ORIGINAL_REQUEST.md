# Original User Request

## Initial Request — 2026-06-07T23:48:44Z

# Teamwork Project Prompt — Draft

> Status: Launched
> Goal: Execute the "Project Origin" Distributed Architecture Core

**Project Origin Framework:** A completely autonomous, self-healing distributed software engine based explicitly on natural laws: The Free Energy Principle (minimizing traffic surprise), Cellular Biology (strict boundaries/Markov blankets), and the Constructal Law (morphing network topology for optimal flow). The system acts as a highly efficient Python-based simulated orchestrator that demonstrates these principles, providing commercial value as a next-gen cloud resource optimizer.

Working directory: c:\Users\ahmad ali\OneDrive\Desktop\Project Origin\origin_core
Integrity mode: development

## Requirements

### R1. Autonomous Node Generation
The system must define a `Node` class that encapsulates its own state (Markov Blanket). Each node should monitor incoming data/requests and maintain a simple generative model of expected traffic.

### R2. Free Energy Minimization (Active Inference)
When a node detects a spike or drop in traffic that deviates from its prediction (high "surprise" or free energy), it must autonomously take action without a central controller. Actions include spawning a sub-node to handle load or throttling the connection.

### R3. Constructal Network Morphing
The network topology must not be static. Based on the flow of data, nodes must periodically re-evaluate their connections and automatically re-wire themselves (e.g., forming a hub-and-spoke for high central load, or a mesh for distributed load) to minimize total network latency/resistance.

## Acceptance Criteria

### Objective Verification
- [ ] Running `python main.py` initiates a network of at least 5 nodes.
- [ ] A simulated load generator sends varying traffic patterns to the network.
- [ ] The terminal output definitively logs nodes detecting high "free energy/surprise" and autonomously spawning/throttling.
- [ ] The terminal output logs the network successfully re-wiring its topology (e.g., node A connects directly to node C) to reduce latency when traffic flow changes.
- [ ] The system recovers stability (homeostasis) after a massive, injected traffic anomaly.
